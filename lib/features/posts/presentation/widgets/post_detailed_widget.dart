import 'package:flutter/material.dart';

class PostDetailedWidget extends StatelessWidget {
  const PostDetailedWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
